server {

    listen 80;

    server_name 0.0.0.0;


    location /media/ {
        alias /code/media/;
    }


    location / {

        proxy_pass http://app:8000;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;

    }

}